/** Shared with vite.config.js's Workbox runtimeCaching cacheName option —
 *  keep both in sync rather than duplicating the literal string. */
export const MENU_IMAGES_CACHE_NAME = 'ayc-menu-images'
